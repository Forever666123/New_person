# ---- 必填 ----
# Discord 机器人 Token（Developer Portal -> Bot -> Reset Token）
# 注意：还要在同一页打开 MESSAGE CONTENT INTENT，否则机器人收到的消息内容永远是空的
DISCORD_BOT_TOKEN=
# Anthropic API Key（anthropic SDK 会自动读这个变量）
ANTHROPIC_API_KEY=
# 你自己的 Discord 用户 ID（开启开发者模式后右键头像 -> 复制用户 ID）
# 机器人只和你说话；你必须和它在同一个服务器里，否则私聊发不出去
OWNER_DISCORD_USER_ID=

# ---- 可选 ----
# 把某个频道也当成同一段对话；留空则只在私聊里
PROACTIVE_CHANNEL_ID=
# 回复和主动消息用的模型。这两件事直接决定她像不像人，别省这里。
NEWPERSON_MODEL=claude-sonnet-5
# 日程生成和记忆整理用的模型。不面向对话，格式对就行，用便宜的那档。
# 留空就跟主模型一样。
NEWPERSON_UTILITY_MODEL=claude-haiku-4-5
# 思考强度：low / medium / high / xhigh / max
# 注意 Haiku 4.5 不接受这个参数，配了会被自动跳过（不会报错）
NEWPERSON_EFFORT=low
# 每天最多调用多少次模型，超了就当"今天没怎么看手机"，不会崩
MAX_CALLS_PER_DAY=200

PERSONA_PATH=persona/persona.yaml
PHOTOS_INDEX=persona/photos/index.yaml
DB_PATH=data/newperson.db

# 调试用：把所有等待时间按比例缩短。0.01 表示一小时变三十六秒。正式用请设回 1.0
DELAY_SCALE=1.0
# 调试用：让她一直醒着。第一次跑起来常常是半夜，她按作息正在睡，
# 你发什么都要等到早上才有反应。看完效果记得关掉
DEBUG_FORCE_AWAKE=0
LOG_LEVEL=INFO
